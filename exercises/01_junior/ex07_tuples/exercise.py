"""
Exercise 07: Tuples

Fix the functions below to make the tests pass.
Look for TODO comments for guidance.
"""


def create_tuple():
    """Return a tuple with elements 1, 2, 3."""
    # TODO: Return a tuple (1, 2, 3)
    pass


def get_first_last(t: tuple) -> tuple:
    """Return a tuple with the first and last elements of t.

    Args:
        t: A non-empty tuple

    Returns:
        A tuple (first_element, last_element)
    """
    # TODO: Return a tuple of (first, last) elements
    pass


def unpack_coordinates(point: tuple) -> tuple:
    """Unpack a 3D coordinate tuple into x, y, z and return them.

    Args:
        point: A tuple (x, y, z)

    Returns:
        A tuple (x, y, z)
    """
    # TODO: Unpack x, y, z from point and return them as a tuple
    pass


def count_occurrences(t: tuple, value) -> int:
    """Count how many times value appears in t.

    Args:
        t: A tuple
        value: The value to count

    Returns:
        The number of times value appears in t
    """
    # TODO: Count occurrences of value in t
    pass


def concatenate_tuples(t1: tuple, t2: tuple) -> tuple:
    """Concatenate two tuples.

    Args:
        t1: First tuple
        t2: Second tuple

    Returns:
        A new tuple with all elements of t1 followed by t2
    """
    # TODO: Concatenate t1 and t2 and return the result
    pass


def tuple_to_list_and_back(t: tuple) -> tuple:
    """Convert a tuple to a list, append 99, then convert back to tuple.

    Args:
        t: A tuple of integers

    Returns:
        A new tuple with 99 appended at the end
    """
    # TODO: Convert t to list, append 99, return as tuple
    pass
